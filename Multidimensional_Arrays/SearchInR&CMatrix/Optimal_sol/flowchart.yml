Start
  |
take input: matrix and target
  |
Initialize i=0, j=n-1 (top-right corner)
  |
While i < n and j >= 0:
  |
  If matrix[i][j] == target:
     Return (i, j)
  Else if matrix[i][j] > target:
     j-=1 (move left)
  Else:
     i+=1 (move down)
  |
If not found, return (-1, -1)
  |
End